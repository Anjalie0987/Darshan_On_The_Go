const { Client } = require('pg'); const client = new Client({ user: 'postgres', host: 'localhost', database: 'darshan', password: 'post123', port: 5433 }); client.connect().then(() => client.query('SELECT * FROM states')).then(res => { console.log(res.rows); client.end(); }).catch(e => { console.error(e); client.end(); });
